<% provide(:title, @text_delivery_and_pay.where(name: "Title").first.value) %>
<% provide(:description, @text_delivery_and_pay.where(name: "Description").first.value) %>
<% provide(:keywords, @text_delivery_and_pay.where(name: "Keywords").first.value) %>
<%= render '/layouts/header/mini_header' %>
<section class="container">
    <section class="grid-holder">
        <section class="grid w-padd">
          <figure class="column c-three-fourth">
            <article>
              <h3>Доставка и оплата</h3>
              <%= @text_delivery_and_pay.where(name:"Доставка и оплата текст").first.value.html_safe %>
            </article>
          </figure>
        </section>
      </section>
  </section>